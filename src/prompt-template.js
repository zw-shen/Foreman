/**
 * 对 agent 的通用要求：与具体任务无关、每个任务都适用的那部分。
 * 可在设置页编辑 —— 这是给你放团队规范、语言偏好、禁止事项的地方。
 *
 * 注意和 {{PROTOCOL}} 的分工：
 * - PROTOCOL 是 Foreman 的机制约定（状态文件写在哪、什么格式），由 Foreman 生成，不可改错
 * - RULES 是你对 agent 干活方式的要求，纯文本，随便改
 */
export const DEFAULT_AGENT_RULES = `- **先看再改。** 动手前先读相关代码和文档，不要凭猜测下手；不确定某段代码的作用就去查，不要假设。
- **改完必须自己验证。** 跑构建、跑测试、跑一遍实际流程，把命令和结果写下来。
  不允许只说"应该没问题"就交付 —— 没验证过就在交接文档里明确写"未验证"。
- **不要擅自扩大范围。** 只做任务要求的事。顺手重构、顺手改风格、顺手升级依赖都不要做；
  觉得确实该做，先问。
- **不确定就停下来问**，把状态写成 \`awaiting_input\` 并写清问题，不要自己猜一个方向闷头做完。
- **少加依赖。** 非必要不引入新依赖；确实要加，说明为什么、以及为什么现有方案不够。
- **跟现有代码保持一致**：命名、目录结构、错误处理、日志方式都照着周围的代码来，不要另起一套。
- **失败就说失败。** 不要粉饰、不要把"绕过了问题"说成"解决了问题"。
  绕过的话要写清绕过的是什么、代价是什么。
- **不许动测试来让结果变好看**：不要删测试、不要跳过测试、不要改断言迁就实现。
- **破坏性操作先问**：删数据、改线上配置、强推 git、重写历史，一律先确认。
- **每完成一步就更新状态并提交**，不要攒到最后 —— 中途没有记录，就无法区分你在干活还是已经挂了。`;

/**
 * 默认提示词模板。可在设置页编辑。
 *
 * 占位符由 Foreman 在生成时替换，其中 {{PROTOCOL}}（协作规范）的内容由 Foreman
 * 根据任务动态生成，模板只决定它出现的位置 —— 这样使用者能调整措辞和顺序，
 * 但规范里的路径、状态取值不会被改错。
 */
export const DEFAULT_PROMPT_TEMPLATE = `你将作为本次任务的执行 agent。请先完整读完本提示词再开始动手。

## 对你的通用要求

{{RULES}}

{{KNOWLEDGE_INDEX}}

{{TASK_INFO}}

{{REPOS}}

{{PROTOCOL}}
`;

export const PLACEHOLDERS = [
  '{{RULES}}',
  '{{KNOWLEDGE_INDEX}}',
  '{{TASK_INFO}}',
  '{{REPOS}}',
  '{{PROTOCOL}}',
];

/** 每个占位符的说明，设置页用来告诉使用者哪些能改、哪些是生成的。 */
export const PLACEHOLDER_HELP = [
  { ph: '{{RULES}}', text: '对 agent 的通用要求，下面那个框里的内容，可自由编辑' },
  { ph: '{{KNOWLEDGE_INDEX}}', text: 'context repo 里的知识条目索引，自动扫出来' },
  { ph: '{{TASK_INFO}}', text: '本任务的目标、背景、验收标准、约束' },
  { ph: '{{REPOS}}', text: 'context repo 地址、本地位置、基线 commit' },
  {
    ph: '{{PROTOCOL}}',
    text: 'Foreman 的机制约定（状态文件路径与格式、HANDOFF 段落要求等），由 Foreman 生成以保证不写错；删掉它整套约定会失效',
  },
];
